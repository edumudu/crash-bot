{
  "name": "crash-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:popup": "mkdir -p ./dist/popup && cp ./src/popup/index.html ./dist/popup/index.html",
    "build:bg": "vite build -c build/vite.background.config.ts",
    "build:content": "vite build -c build/vite.content.config.ts",
    "build": "pnpm build:bg && pnpm build:content && pnpm build:popup"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.0.217",
    "typescript": "^4.9.5",
    "vite": "^4.1.4"
  }
}
